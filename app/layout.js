export const metadata = {
  title: "VBEATS",
  description: "Gen-Z Advanced Photo | Video | Audio Editing Platform",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
